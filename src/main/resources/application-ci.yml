org:
  neo4j:
    driver:
      uri: ${NEO4J_URI}
      authentication:
        username: ${NEO4J_USER}
        password: ${NEO4J_PASS}

llf:
  api:
    here:
      uri: https://localhost
      port: 0
      key: 12345
    transport:
      uri: http://localhost
      port: 0
      key: abcde